const HeaderStyles = () => {
  const styles = {
    header: {
      display: 'flex',
      flexDirection: 'row',
      justifyContent: 'space-between',
      alignItems: 'center',
      margin: '2rem 2rem 0',
    },
  };
  return styles;
};

export default HeaderStyles;
