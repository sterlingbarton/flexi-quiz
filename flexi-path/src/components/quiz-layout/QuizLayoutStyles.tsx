const QuizLayoutStyles = () => {
  const styles = {
    layoutContainer: {
      display: 'flex',
      width: '100vw',
      height: '100vh',
      flexDirection: 'column',
      overflowX: 'hidden',
    },
  };
  return styles;
};

export default QuizLayoutStyles;
