const LogoStyles = () => {
  const styles = {
    logo: {
      height: 20,
      width: 'auto',
      '&:hover': {
        cursor: 'pointer',
      },
    },
  };
  return styles;
};

export default LogoStyles;
